{
  "name": "jots-footlinker",
  "version": "1.0.0",
  "description": "A plugin for Obsidian that adds footers to notes with useful information.",
  "main": "src/main.js",
    "scripts": {
      "build": "esbuild src/main.js --bundle --outfile=dist/main.js --external:obsidian --format=cjs",
      "watch": "esbuild src/main.js --bundle --outfile=dist/main.js --watch --external:obsidian --format=cjs"
    },
  "keywords": [
    "obsidian",
    "plugin",
    "footlinker"
  ],
  "author": "Your Name",
  "license": "MIT",
  "dependencies": {
    "obsidian": "^0.12.0"
  },
  "devDependencies": {
    "esbuild": "^0.12.0"
  }
}